#include<bits/stdc++.h>
using namespace std;

int main(){
    string s = "Prince";
    int len = s.size();  // returns the length of the array
    cout << "Length of the array: " << len;
    return 0;
}